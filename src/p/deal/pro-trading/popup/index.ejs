<div class="popup <% if(data.up){%> up <%}%> ">
	<div class="hd">
		<span class="warpper-icon-outer">
	    <span class="wrapper-icon"><span class="icon"></span></span>
	  </span>
    <h2><%= data.name %></h2>
    <span class="J_ClosePopup close-outer">
    	<span class="close"></span>
    </span>
  </div>
  <ul class="clearfix">
  	<li class="item">交易量（手）：<span class="large"><%= data.volume %></span></li>
  	<li class="item">开仓价格（$）：<span class="large"><%= parseFloat(data.openPrice).toFixed(data.minQuoteUnit) %></span><span class="bar"></span></li>
  	<li class="item one">目标/止损（$）：<span class="large"><%= data.takeProfit == 0 ? '--' : parseFloat(data.takeProfit).toFixed(data.minQuoteUnit)  %> / <%= data.stopLoss == 0 ? '--' : parseFloat(data.stopLoss).toFixed(data.minQuoteUnit)  %></span></li>
  	<li class="item">占用资金（$）：<span class="large"><%= data.margin %></span></li>
  	<li class="item">建仓方法：<span class="large"><%= data.guadan ? '挂单' : '现价' %></span><span class="bar"></span></li>
  </ul>
  <div class="dialog-buttons clearfix" id="J_Success">
       <a class="dialog-btn order J_Order" href="../trade.html">查看我的交易</a>
       <a class="dialog-btn list J_List" href="../option.html">返回商品列表</a>
   </div>
</div>
